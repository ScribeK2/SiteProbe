<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.5; color: #111; }
    .container { max-width: 600px; margin: 0 auto; padding: 40px 20px; }
    .header { font-size: 14px; color: #666; margin-bottom: 30px; }
    h1 { font-size: 24px; font-weight: 300; margin-bottom: 20px; }
    h2 { font-size: 18px; font-weight: 400; margin: 30px 0 15px; }
    .summary { padding: 20px; background: #f9fafb; border-radius: 8px; margin-bottom: 30px; }
    .stat { display: inline-block; margin-right: 30px; }
    .stat-value { font-size: 32px; font-weight: 300; }
    .stat-label { font-size: 12px; color: #666; }
    .item { padding: 15px; border-bottom: 1px solid #eee; }
    .item:last-child { border-bottom: none; }
    .item-domain { font-family: monospace; font-weight: 500; }
    .item-days { font-size: 14px; color: #666; }
    .item-days.urgent { color: #ef4444; }
    .item-days.warning { color: #f59e0b; }
    .button { display: inline-block; padding: 12px 24px; background: #000; color: #fff; text-decoration: none; border-radius: 4px; margin-top: 20px; }
    .footer { margin-top: 40px; padding-top: 20px; border-top: 1px solid #eee; font-size: 12px; color: #666; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">SiteProbe</div>
    
    <h1>Weekly Expiration Summary</h1>
    
    <div class="summary">
      <div class="stat">
        <div class="stat-value"><%= @expiring_domains.length %></div>
        <div class="stat-label">Domain<%= @expiring_domains.length != 1 ? 's' : '' %> Expiring</div>
      </div>
      <div class="stat">
        <div class="stat-value"><%= @expiring_ssl.length %></div>
        <div class="stat-label">SSL Certificate<%= @expiring_ssl.length != 1 ? 's' : '' %> Expiring</div>
      </div>
    </div>
    
    <% if @expiring_domains.any? %>
      <h2>Domain Registrations</h2>
      <div style="background: #f9fafb; border-radius: 8px;">
        <% @expiring_domains.each do |item| %>
          <div class="item">
            <div class="item-domain"><%= item[:domain] %></div>
            <div class="item-days <%= item[:days_until] <= 7 ? 'urgent' : (item[:days_until] <= 30 ? 'warning' : '') %>">
              Expires in <%= item[:days_until] %> days
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
    
    <% if @expiring_ssl.any? %>
      <h2>SSL Certificates</h2>
      <div style="background: #f9fafb; border-radius: 8px;">
        <% @expiring_ssl.each do |item| %>
          <div class="item">
            <div class="item-domain"><%= item[:domain] %></div>
            <div class="item-days <%= item[:days_until] <= 7 ? 'urgent' : (item[:days_until] <= 30 ? 'warning' : '') %>">
              Expires in <%= item[:days_until] %> days
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
    
    <a href="<%= dashboard_url %>" class="button">View Dashboard →</a>
    
    <div class="footer">
      <p>You're receiving this weekly summary because you have domains or SSL certificates expiring within 90 days.</p>
    </div>
  </div>
</body>
</html>

