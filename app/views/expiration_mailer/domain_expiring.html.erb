<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.5; color: #111; }
    .container { max-width: 600px; margin: 0 auto; padding: 40px 20px; }
    .header { font-size: 14px; color: #666; margin-bottom: 30px; }
    h1 { font-size: 24px; font-weight: 300; margin-bottom: 20px; }
    .alert { padding: 20px; border-left: 4px solid #ef4444; background: #fef2f2; margin-bottom: 30px; }
    .alert-title { font-weight: 500; margin-bottom: 5px; }
    .alert-message { color: #666; font-size: 14px; }
    .domain { font-family: monospace; font-size: 18px; margin: 20px 0; }
    .button { display: inline-block; padding: 12px 24px; background: #000; color: #fff; text-decoration: none; border-radius: 4px; margin-top: 20px; }
    .footer { margin-top: 40px; padding-top: 20px; border-top: 1px solid #eee; font-size: 12px; color: #666; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">SiteProbe</div>
    
    <h1>Domain Expiring Soon</h1>
    
    <div class="alert">
      <div class="alert-title">⚠️ Action Required</div>
      <div class="alert-message">Your domain registration expires in <%= @days_until_expiry %> days.</div>
    </div>
    
    <div class="domain"><%= @domain %></div>
    
    <p>To avoid service disruption, please renew your domain registration as soon as possible.</p>
    
    <a href="<%= check_url(@domain_check) %>" class="button">View Full Report →</a>
    
    <div class="footer">
      <p>You're receiving this because you have notifications enabled for domain expirations on SiteProbe.</p>
    </div>
  </div>
</body>
</html>

